export const ingredients = (state) => state.ingredients

export const ingredient = (state) => state.ingredient

export const userEntities = (state) => state.userEntities

export const newIngredientModal = (state) => state.newIngredientModal

export const selectedCompany = (state) => state.selectedCompany
