export const chartAccounts = (state) => state.chartAccounts

export const chartAccount = (state) => state.chartAccount

export const companies = (state) => state.companies

export const company = (state) => state.company

export const newChartAccount = (state) => state.newChartAccount

export const parentAccount = (state) => state.parentAccount

export const editChartAccount = (state) => state.editChartAccount

export const parentAccountName = (state) => state.parentAccountName

export const tAccounts = (state) => state.tAccounts
