export const accountingStandard = ({
  commit
}, payload) => {
  commit('accountingStandard', payload)
}

export const accountingStandardName = ({
  commit
}, payload) => {
  commit('accountingStandardName', payload)
}

export const accountingStandardMask = ({
  commit
}, payload) => {
  commit('accountingStandardMask', payload)
}

export const newAccountingStandard = ({
  commit
}, payload) => {
  commit('newAccountingStandard', payload)
}
