export const branches = (state) => state.branches

export const branch = (state) => state.branch

export const companies = (state) => state.companies

export const newBranchModal = (state) => state.newBranchModal
